# MD Circle of the Moon Assets

Curated public asset repository for pixel-art collaboration.

This repository contains only the `res/` assets that are currently wired into the project resource declaration (`res/resources.res`). It intentionally does not include source code, tools, generated build output, or unused/experimental asset files.

The main development repository remains private.
